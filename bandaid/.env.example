# Bandaid Environment Variables
# Copy this file to .env and fill in your values
# Note: Most configuration is done via config/config.yaml, but environment
# variables can be used for sensitive values or deployment-specific settings.

# ============================================================================
# LLM Provider API Keys
# ============================================================================
# These can be used instead of storing API keys in config.yaml
# Reference in config.yaml with: api_key = "${OPENAI_API_KEY}"

# OpenAI
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Anthropic Claude
# Get your API key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini
# Get your API key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=...

# Cohere
# Get your API key at: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=...

# Azure OpenAI
AZURE_API_KEY=...
AZURE_API_BASE=https://your-resource.openai.azure.com/
AZURE_API_VERSION=2024-02-01

# ============================================================================
# HuggingFace
# ============================================================================
# Required for downloading Llama Guard 3 model (requires access approval)
# Get your token at: https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=hf_...

# ============================================================================
# Observability
# ============================================================================

# Sentry (optional - for error tracking and monitoring)
# Get your DSN at: https://sentry.io/
SENTRY_DSN=https://...@sentry.io/...
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1

# ============================================================================
# Application Settings
# ============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Log format (json, text)
LOG_FORMAT=json

# ============================================================================
# Server Configuration
# ============================================================================

# Proxy server settings
PROXY_HOST=0.0.0.0
PROXY_PORT=8000

# Dashboard settings
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=8001

# ============================================================================
# Database Configuration
# ============================================================================

# SQLite database path
SQLITE_PATH=./data/events.db

# ChromaDB path
CHROMADB_PATH=./data/chroma

# Event retention in days
RETENTION_DAYS=30

# ============================================================================
# Model Configuration
# ============================================================================

# Device for model inference (auto, cpu, cuda, mps)
# "auto" will automatically detect best available device
MODEL_DEVICE=auto

# Lazy load models (true/false)
# When true, models are loaded on first use instead of at startup
LAZY_LOAD=true

# ============================================================================
# Security Configuration
# ============================================================================

# Enable/disable specific security checks
NER_ENABLED=true
GUARD_ENABLED=true
REGEX_ENABLED=true
EMBEDDINGS_ENABLED=true
SEED_PHRASE_ENABLED=true

# Confidence thresholds (0.0-1.0)
CONFIDENCE_HIGH=0.9
CONFIDENCE_MEDIUM=0.7
CONFIDENCE_LOW=0.5

# ============================================================================
# Development Settings
# ============================================================================

# Enable development mode (auto-reload, verbose logging)
DEV_MODE=false

# Enable CORS for development
CORS_ENABLED=true

# ============================================================================
# Notes
# ============================================================================
#
# 1. API Keys:
#    - Store API keys here for security (not in config files)
#    - Reference in config.yaml with: api_key = "${OPENAI_API_KEY}"
#    - Keys are encrypted when stored in config.yaml via CLI
#
# 2. Environment Priority:
#    - Environment variables override config.yaml values
#    - Use for deployment-specific settings
#
# 3. HuggingFace Token:
#    - Required for Llama Guard 3 (access approval needed)
#    - Login: huggingface-cli login
#    - Or set HUGGINGFACE_TOKEN environment variable
#
# 4. Sentry (Optional):
#    - Leave SENTRY_DSN empty to disable
#    - Sends errors and performance data to Sentry
#
# 5. Device Configuration:
#    - auto: Auto-detect best available (CUDA > MPS > CPU)
#    - cuda: Force NVIDIA GPU (requires CUDA)
#    - mps: Force Apple Silicon GPU (macOS only)
#    - cpu: Force CPU (slowest but most compatible)
#
# 6. Security:
#    - Keep this file out of version control (.gitignore)
#    - Never commit real API keys
#    - Rotate keys regularly
#
# For more information, see:
# - Documentation: https://docs.bandaid.dev
# - Quickstart: specs/001-llm-security-proxy/quickstart.md
