"""Setup script for Bandaid LLM Security Proxy.

This setup.py exists for compatibility with older build tools and pip install -e.
Modern packaging uses pyproject.toml (PEP 517/518).
"""

from setuptools import setup

# All configuration is in pyproject.toml
# This file is kept minimal for backwards compatibility
setup()
