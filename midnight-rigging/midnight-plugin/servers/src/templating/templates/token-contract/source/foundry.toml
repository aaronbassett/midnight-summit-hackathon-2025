# Foundry Configuration
# Generated by {{TEMPLATE_NAME}} v{{TEMPLATE_VERSION}} on {{CURRENT_DATE}}

[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
solc = "0.8.28"
evm_version = "prague"
bytecode_hash = "none"
cbor_metadata = false

# After running 'forge install podnetwork/pod-sdk', Foundry will automatically
# create the necessary remappings in remappings.txt

[fmt]
exclude = ["lib/**"]

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
