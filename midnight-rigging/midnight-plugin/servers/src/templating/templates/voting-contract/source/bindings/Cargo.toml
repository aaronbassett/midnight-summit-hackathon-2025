[package]
name = "{{bindings_crate_name}}"
version = "0.1.0"
edition = "2021"
authors = ["{{author}}"]

# Generated by {{TEMPLATE_NAME}} v{{TEMPLATE_VERSION}} on {{CURRENT_DATE}}
#
# This crate contains auto-generated Rust bindings for the {{contract_name}} contract.
# The src/ directory is populated by running 'make generate' in the contract/ directory.

[dependencies]
alloy = { version = "1.0.36", features = ["sol-types", "contract"] }
serde = { version = "1.0", features = ["derive"] }
